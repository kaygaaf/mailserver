# Docker Mailserver Configuration
# Full docs: https://docker-mailserver.github.io/docker-mailserver/latest/config/environment/

# Hostname
OVERRIDE_HOSTNAME=mail.kayorama.nl

# SSL/TLS
SSL_TYPE=letsencrypt

# Postmaster
POSTMASTER_ADDRESS=admin@kayorama.nl

# Authentication
ENABLE_SASLAUTHD=1
SASLAUTHD_MECHANISMS=rimap
SASLAUTHD_MECH_OPTIONS=localhost

# Security
ENABLE_FAIL2BAN=1
FAIL2BAN_BLOCKTYPE=drop

# Spam/Antivirus
ENABLE_SPAMASSASSIN=1
SPAMASSASSIN_SPAM_TO_INBOX=1
ENABLE_CLAMAV=1

# POP3/IMAP
ENABLE_POP3=0
ENABLE_IMAP=1

# Logging
LOG_LEVEL=info
SUPERVISOR_LOG_LEVEL=warn

# Network
PERMIT_DOCKER=connected-networks

# Mailbox
ONE_DIR=1
DMS_DEBUG=0

# Accounts (create with: docker exec mailserver setup email add user@domain.com password)
# Aliases (create with: docker exec mailserver setup alias add alias@domain.com user@domain.com)
