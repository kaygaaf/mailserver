# Mailu Email Server - Environment Variables
# Set these in Coolify before deploying

# Required: Your main domain
DOMAIN=kayorama.nl

# Optional: Mail server hostname (defaults to mail.${DOMAIN})
# HOSTNAMES=mail.kayorama.nl

# Optional: Admin password (auto-generated if not set)
# ADMIN_PASSWORD=your-secure-password

# Optional: Secret key (auto-generated if not set)
# SECRET_KEY=$(openssl rand -hex 32)

# Optional: Additional domains (comma-separated)
# OTHER_DOMAINS=domain2.com,domain3.com
